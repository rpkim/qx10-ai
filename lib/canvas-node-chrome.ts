/**
 * Toolbar strip above each canvas node (`NodeChrome`: h-6 + mb-1).
 * Layout, edges, and minimap must include this so spacing and anchors match the DOM.
 */
export const NODE_CANVAS_TOOLBAR_HEIGHT_PX = 28;
